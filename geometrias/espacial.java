import java.util.Scanner;
import java.util.*;
import static java.lang.Math.PI;



class esfera {
    double PI, raio, areaSuperficial; //soma, sub;
    areaSuperficial = 4 * PI * (Math.pow(raio, 2));
     //4 pi r^2
    volume = (4 / 3) * PI * (Math.pow(raio, 3));
}

